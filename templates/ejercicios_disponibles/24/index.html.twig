{% extends 'entrenamiento.html.twig' %}
{% block title %}Título de mi juego{% endblock %}
{% block css %}
  <style media="screen">
    img{
      height: 15%;
      width: 15%;
      margin:10px;
    }
  </style>
{% endblock %}
{% block juego %}
  <img src="/img/ejercicios/editada.jpg" id="elejida" class="principal">
  <br>
  <br>
  <br>
  <img src="/img/ejercicios/editada3.jpg" onclick="comprobar(event)" class="opciones">
  <img src="/img/ejercicios/editada.jpg" onclick="comprobar(event)" class="opciones">
  <img src="/img/ejercicios/editada3.jpg" onclick="comprobar(event)" class="opciones">
  <img src="/img/ejercicios/editada3.jpg" onclick="comprobar(event)" class="opciones">
{% endblock %}
{% block funcionalidad %}
  <script type="text/javascript">
    var parrafoPuntos, parrafoNivel, inputNivel, inputPuntos;
    var puntos, nivel;

    function load() {
      parrafoPuntos = document.getElementById('puntos');
      inputPuntos = document.getElementById('inputPuntos');
      puntos = parrafoPuntos.innerHTML;
      inputPuntos.value = puntos;
      parrafoNivel = document.getElementById('nivel');
      inputNivel = document.getElementById('inputNivel');
      nivel = parrafoNivel.innerHTML;
      inputNivel.value = nivel;
      elejida = document.getElementById('elejida');
    }

    function comprobar(ev) {
      console.log(ev.target.src);
      if (ev.target.src == elejida.src) {
        puntos ++;
        nivel ++;
        actualizarPuntos();
        actualizarNivel();
        //cambiar imagenes
      }
    }

    function actualizarPuntos() {
      parrafoPuntos.innerHTML=puntos;
      inputPuntos.value = puntos;
    }
    function actualizarNivel() {
      parrafoNivel.innerHTML = nivel;
      inputNivel.value = nivel;
    }

    function empezarCero() {
      puntos = 0;
      nivel = 0;
      actualizarNivel();
      actualizarPuntos();
    }
  </script>
{% endblock %}
