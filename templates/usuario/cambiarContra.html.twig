{% extends 'base.html.twig' %}

{% block title %}Cambiar contraseña{% endblock %}

{% block body %}
<section class="py-5">
  <div class="container px-5">
    <!-- Contact form-->
      <div class="bg-light rounded-3 py-5 px-4 px-md-5 mb-5">
        <div class="text-center mb-5">
          <h1 class="fw-bolder">Cambiar contraseña:</h1>
        </div>
        <div class="row gx-5 justify-content-center">
          <div class="col-lg-8 col-xl-8">
            {{ form_start(changePasswordForm) }}
                {{ form_row(changePasswordForm.correo) }}
                {{ form_row(changePasswordForm.plainPassword, {
                    label: 'Escriba la nueva contraseña:'
                }) }}
                <div class="d-grid"><button class="btn btn-primary btn-lg" id="submitButton" type="submit">Cambiar contraseña</button></div>
            {{ form_end(changePasswordForm) }}
          </div>
        </div>
      </div>
    </div>
  </section>
{% endblock %}
